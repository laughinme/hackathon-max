# Only for `npm run dev` in a regular browser, outside MAX: the API is called
# with `Authorization: dev <id>`. The backend accepts it only with
# DEV_AUTH_ENABLED=true. Demo resident with tickets: see backend seed; any id
# becomes a dispatcher via the bot's demo button.
VITE_DEV_USER_ID=
