# Данные: модель, нормативный справочник, синтетика (черновик)

Статус: черновик под [PRODUCT.md](PRODUCT.md), уточняется по мере кода. Реальных интеграций (ГИС ЖКХ, АДС, регоператор) в MVP нет — только модельные данные, это явно указывается в README, на слайде и в боте (требование PDF 7, 18).

## 1. Доменная модель

| Агрегат / сущность | Ключевые поля | Комментарий |
|---|---|---|
| `ManagementCompany` (УО) | id, name, inn (синтетический), contacts, region_code, dispatch_hours | Одна демо-УО «Комфорт», 50 домов |
| `Building` (дом) | id, short_code (для QR/диплинка), address, fias_id (модельный), region_code, company_id, entrances, floors, has_elevator, year_built | 50 домов, 3 из них с «живыми» чатами |
| `ChatBinding` | chat_id (MAX), building_id, bot_is_admin, can_read_all, bound_at, status | Реестр чатов ведём сами (`GET /chats` в MAX удалён) |
| `Resident` | id, max_user_id, display_name (из профиля MAX), created_at | Никаких ФИО и телефонов сверх того, что отдаёт MAX |
| `Membership` | resident_id, building_id, role (`owner` / `tenant` / `chair` / `dispatcher`), apartment (опционально, вводит сам), source (`chat` / `qr` / `seed`) | Роль `dispatcher` привязана к УО, не к дому |
| `Ticket` (заявка) | id, number (`<год>-<seq>` или `<код дома>-<seq>`), building_id, reporter_id, category, subcategory, responsible_party, is_emergency, description, description_source (`resident` / `llm_dialog`), ai_photo_summary, classification_confidence, photos[], status, created_at, react_by, resolve_by, legal_basis, closed_at, chat_message_mid (сообщение-карточка в чате), source (`chat` / `dialog` / `miniapp`) | Агрегат; переходы только через `state_machine`. `is_emergency` — выход ML-классификатора, не ручной ввод; ML-поля — см. §7 |
| `TicketSupport` | ticket_id, resident_id, created_at | «Я тоже»; уникальность (ticket, resident) |
| `TicketEvent` | ticket_id, type, actor (resident / dispatcher / system), payload, created_at | Таймлайн: created, acknowledged, in_progress, comment, done, confirmed, reopened, overdue, escalated |
| `Escalation` | реализовано полями заявки `overdue_notified_at`, `escalated_at` ([D-012](DECISIONS.md)) | PDF собирается при отправке из текущей истории, не хранится |
| `InboxEvent` | dedup_key, update_type, payload, received_at, processed_at, error | Идемпотентность вебхука |
| `OutboxMessage` | id, target (user_id / chat_id), body, dedup_key, status, attempts, mid, next_attempt_at | Транзакционный outbox |
| `DialogState` | max_user_id, chat_id, flow, step, data, updated_at | Состояние пошаговых форм в БД |
| `BuildingDigest` | building_id, period, stats | Кэш «пульса дома» |

### Статусы заявки
Реализовано (`domain/tickets/state_machine.py`, [D-008](DECISIONS.md)): `registered` (номер и срок присваиваются сразу) → `acknowledged` → `in_progress` → `done` → `confirmed`; житель может вернуть `done` → `in_progress` (переоткрытие); диспетчер может перевести любую открытую заявку в `rejected`. Двигает работу только диспетчер, подтверждает или переоткрывает только автор заявки. `overdue` — вычисляемое свойство (открыта и `now > resolve_by`), не статус. Эскалация в ГЖИ — отдельная сущность (шаг 5).

## 2. Нормативный справочник сроков (SLA)

Реализовано кодом в `backend/src/domain/tickets/sla.py` (таблица `ROUTINE_RULES`, `EMERGENCY_RULE`, `FALLBACK_RULE`), каждая запись — со ссылкой на пункт документа, тесты — `tests/unit/domain/test_sla.py`. Рабочие дни — `calendar.py`: праздники по ст. 112 ТК РФ, переносы выходных на конкретный год пока не учтены. **Все значения ниже — из памяти и вторичных источников, до 25.09 сверить с текстом первоисточника (КонсультантПлюс/Гарант) и поправить**; ответственный — роль «продукт» (DECISIONS Q-13).

| Категория (пример) | Аварийность | Срок реакции / устранения | Основание (проверить) |
|---|---|---|---|
| Регистрация любой заявки в АДС | — | номер и срок сообщаются заявителю при регистрации | ПП РФ № 416, п. 17 |
| Ответ АДС на звонок | — | 5 мин, либо обратный звонок в течение 10 мин | ПП 416, п. 13 |
| Авария внутридомовых систем ХВС/ГВС/отопления/электро/канализации | да | локализация ≤ 30 мин с регистрации; устранение ≤ 3 суток | ПП 416, п. 13 |
| Засор канализации / мусоропровода | да | ≤ 2 ч | ПП 416, п. 13 |
| Отсутствие холодной или горячей воды | да | допустимый перерыв 8 ч суммарно в месяц, 4 ч единовременно (24 ч при аварии на тупиковой магистрали) | ПП РФ № 354, прил. 1 |
| Отсутствие отопления | да | 24 ч суммарно в месяц; единовременно ≤ 16 ч при t ≥ 12 °C, ≤ 8 ч при 10–12 °C, ≤ 4 ч при 8–10 °C | ПП 354, прил. 1 |
| Отсутствие электроснабжения | да | 2 ч при двух источниках питания, 24 ч при одном | ПП 354, прил. 1 |
| Неисправность лифта | нет (если никто не заблокирован) | ≤ 1 сутки | Правила Госстроя № 170 (27.09.2003), прил. 2 |
| Протечка кровли | нет | ≤ 1 сутки | ПП 170, прил. 2 |
| Освещение мест общего пользования | нет | ≤ 7 суток | ПП 170, прил. 2 |
| Разбитые окна/двери в подъезде | нет | 1 сутки зимой, 3 суток летом | ПП 170, прил. 2 |
| Неисправность входной двери, домофона (общее имущество) | нет | ≤ 1 сутки | ПП 170, прил. 2 (уточнить) |
| Информационный запрос в УО (реквизиты, тарифы) | — | 1 рабочий день | ПП 416, пп. 34–36 ([источник](https://roskvartal.ru/deyatelnost-uo/sroki-otvetov-uo-na-obrascheniya-zhiteley-domov-i-storonnih-lic)) |
| Расширенный запрос сведений | — | 3 рабочих дня | ПП 416, пп. 34–36 |
| Иное обращение в УО | — | 10 рабочих дней | ПП 416, пп. 34–36 |
| Обращение в ГЖИ / орган власти | — | 30 дней | 59-ФЗ, ст. 12 |
| Обращение к РСО по качеству ресурса | — | проверка факта нарушения качества — по ПП 354 разд. X (сроки уточнить) | ПП 354 |

Правила расчёта: рабочие дни по производственному календарю РФ (справочник праздников в конфиге), часовой пояс дома (регион), аварийные сроки — в календарных часах. Функция чистая, покрыта тестами на каждую строку таблицы.

## 3. Матрица ответственности («кто отвечает»)

| Тип проблемы | Ответственный | Основание (проверить) |
|---|---|---|
| Общее имущество: подъезд, кровля, лифт, стояки до первого отключающего устройства, освещение, придомовая территория в границах участка | УО / ТСЖ | ЖК РФ ст. 161, ПП РФ № 491 п. 2–7 |
| Внутри квартиры после первого отключающего устройства, розетки, смесители | собственник (УО — платно) | ПП 491 п. 5 |
| Качество и подача ресурса до границы дома (давление, температура, отключения по сетям) | РСО (заявка через УО/АДС) | ПП 354 |
| Плановый капремонт, программа и сроки | региональный оператор / фонд капремонта | ЖК РФ разд. IX |
| Дороги и тротуары за границей участка, уличное освещение, ливнёвка | муниципалитет (ПОС «Решаем вместе») | 131-ФЗ |
| Вывоз ТКО (контейнерная площадка вне зоны УО) | региональный оператор ТКО | 89-ФЗ |

## 4. Синтетический набор данных

- 1 УО («Комфорт», Псков, синтетический ИНН формата `60xxxxxxxx`), 50 домов с реалистичными, но **вымышленными** адресами (улицы существуют, номера домов — нет, или пометка «демо»), 3 дома с домовыми чатами (тестовые группы команды).
- ~200 заявок за последние 90 дней: распределение по категориям (лифт 15%, вода 20%, отопление 10%, освещение 15%, уборка/подъезд 20%, кровля 5%, прочее 15%), доля просроченных ~25%, доля подтверждённых ~60%; несколько заявок с «+N соседей».
- Жители: 30 синтетических имён (`Демо Житель 12`), роли распределены; 1 диспетчер, 1 председатель.
- Генерация: детерминированный сид (`--seed 42`), команда `make seed`; данные помечены полем `is_demo=true`, в UI — бейдж «демо-данные».
- Сброс демо: команда `make demo-reset` (для повторных прохождений проверяющими).

## 5. Персональные данные и правила

- Храним только `max_user_id`, отображаемое имя из MAX, номер квартиры (если ввёл сам), тексты заявок и фото, которые пользователь отправил сознательно. Телефон — не запрашиваем в MVP.
- Пользователь при первом действии принимает условия и политику обработки ПДн (команда `/privacy`, экран в мини-приложении) — требование правил платформы.
- В демо и тестах — только синтетические данные; выгрузки реальных чатов — в `case/raw/` (в `.gitignore`), в репозиторий не попадают.
- Логи без текста сообщений пользователей и без токенов.

## 6. Источники данных для масштабирования (описать в слайде, не интегрировать)

ГИС ЖКХ (дом → УО, лицензии), ГАР/ФИАС (адреса), реестр региональных операторов капремонта, контакты ГЖИ регионов, производственный календарь. Формат импорта — CSV в `infrastructure/seed/`, чтобы регион подключался конфигом.

## 7. ML-классификация жалоб

Решение — [DECISIONS.md D-005](DECISIONS.md#d-005-ml-классификация-жалоб-категория-и-аварийность-через-catboost-llm-для-фото-и-уточнений-ml-lora-удалён-2209-ml-инженер-команды). Конвейер на создании заявки:

1. Текст → **CatBoost**-классификатор → `category`/`subcategory` **и** `is_emergency`, оба с `classification_confidence`. `is_emergency` — не ручной ввод и не статическая константа категории (как сейчас в `catalog.py`), это то, что различает, например, «лифт не работает» (не авария) от «лифт застрял, внутри человек» (авария) — см. справочник §2. Оба выхода — прямой вход в `SlaPolicy(category, is_emergency, ...)`, который и считает `resolve_by`/`legal_basis`. Отдельной модели «приоритета для сортировки» не нужно: очередь диспетчера сортируется по остатку времени до `resolve_by` (`sort=sla`), это уже даёт правильный порядок, если `category`/`is_emergency` определены верно.
2. Если есть фото и уверенность в норме → LLM через API по фото + тексту генерирует `ai_photo_summary` (доп. описание для диспетчера, исходный `description` не меняется).
3. Если `classification_confidence` (по категории или по `is_emergency`) ниже порога (значение — открытый вопрос, Q-09) → LLM-диалог задаёт уточняющие вопросы / просит фото → итоговый текст заменяет `description`, `description_source = llm_dialog`. Fail-safe: при низкой уверенности именно в `is_emergency` заявка по умолчанию регистрируется как аварийная, пока диалог не уточнит обратное.

Для обучения CatBoost нужен размеченный корпус **текстов** жалоб по категориям и признакам аварийности — синтетика из §4 сейчас задаёт только распределение по категориям, не сами формулировки; корпус предстоит собрать или сгенерировать отдельно.
